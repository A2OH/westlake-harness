<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.westlake.probe.defaultinterface"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk android:minSdkVersion="28" android:targetSdkVersion="34" />

    <application
        android:name=".ProbeApplication"
        android:allowBackup="false"
        android:label="Westlake Service Metadata Probe"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <service
            android:name=".MetadataService"
            android:exported="false">
            <meta-data
                android:name="org.westlake.probe.REGISTRAR"
                android:value="org.westlake.probe.ComponentRegistrar" />
            <meta-data
                android:name="org.westlake.probe.SECOND"
                android:value="second-value" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
